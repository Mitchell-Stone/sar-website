import React from 'react'
import Typography from '@material-ui/core/Typography';
import Grid from '@material-ui/core/Grid';

export default function Index() {
  return (
    <Grid container>
      <Typography variant="h2">Sub HERO for explaining who we are!</Typography>
      <Typography>Then a sentence or two to cover off on the topic a bit more</Typography>
    </Grid>
  )
}
